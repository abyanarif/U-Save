import 'package:flutter/material.dart';

class MutasiTransaksi extends StatelessWidget {
  const MutasiTransaksi({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [Center(child: Text('Ini adalah halaman Mutasi!'))],
    );
  }
}
